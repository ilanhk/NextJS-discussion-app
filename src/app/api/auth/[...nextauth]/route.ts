export { GET, POST } from "@/auth"

//we only need to create these if we need an outside server  to access our app programatically.
//dont need this if using our own server.
//Github servers will access our app for authentication for different points in time
